import schema from './schema.js';

const metadata = {
  name: '@engine9/interfaces/task',
  version: '1.0.0',
  schemas: ['schema.js']
};

export { metadata };
export { schema };
export default {
  metadata,
  schema
};
